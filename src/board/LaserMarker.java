package board;

/**
 * PlaceHolder class for laser Markers
 * @author Asad Khan
 *
 */

public class LaserMarker extends BoardEntity {
	
	/**
	 * Constructor that inherits the superclass BoardEntity 
	 * @param x sets the x coordinate of the marker
	 * @param y sets the y coordinate of the marker
	 * @param repr is a String defining the representation
	 */

	public LaserMarker(int x, int y, String repr) {
		super(x, y, repr);
		// place holder class
	}

}
